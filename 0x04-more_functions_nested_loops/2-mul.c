#include "main.h"

/**
 * Description: mul - Multiplies two integers.
 * @a: First integer
 * @b: Second integer
 * Return: Returns multiple of both integers.
 */

int mul(int a, int b)
{
	return (a * b);
}

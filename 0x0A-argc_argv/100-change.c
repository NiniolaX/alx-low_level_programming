#include <stdio.h>

/**
 * main - Prints the minimum number of coins to make change for an amount of
 * money
 * @argc: number of arguments received
 * @argv: pointer to string of arguments received
 * Return: 0 if success
 */

int main(int argc, char **argv)
{
	int cent_denom[] = {25, 10, 5, 2, 1};
	int i;

	if (argc = 2)
	{
		for(i = 0; i
	}
	else
	{
		print("Error\n");
		return (1);
	}
}
